services:
  frontend:
    image: ghcr.io/PascalNehlsen/conduit-frontend:${IMAGE_TAG}
    ports:
      - 8282:80
    restart: unless-stopped
    networks:
      - conduit-network

  backend:
    image: ghcr.io/PascalNehlsen/conduit-backend:${IMAGE_TAG}
    env_file:
      - .env
    volumes:
      - conduit-backend:/app/db_data
    ports:
      - 8000:8000
    restart: unless-stopped
    networks:
      - conduit-network

volumes:
  conduit-backend:

networks:
  conduit-network:
    driver: bridge
